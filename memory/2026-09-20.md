# memory/2026-09-20.md — the Linux day, the v2 design, and 1.1.0 → 1.1.2

The longest day in the log, and the one most worth re-reading. A fresh-device verification on
Linux exposed the v0.2 platform debt; `specs/v2-architecture.md` was written and decisions
D1–D6 locked; 1.1.0 closed cross-platform support and the worker's denied-Bash defect (F5);
1.1.1 fixed a win32 path bug caught only by running the suite on Windows; 1.1.2 closed **F6**,
where `glm-review`'s read-only guarantee was defeated by inherited MCP tools. Also holds the
harness lessons from real worker runs.

**Note the date collision:** the v2 Phase A–D entries are *also* dated 2026-09-20 but live in
`MEMORY.md`, not here — this file is pre-v2 work only.

Archived from `MEMORY.md` on 2026-09-21. These entries are **settled history**: every version
named below is published to npm, verified from the registry and tagged. Open threads stayed in
`MEMORY.md`.

| Date | What | Commits |
|---|---|---|
| 2026-09-20 | Wrote the v2 build spec `specs/v2-architecture.md` from `docs/GLM Coding Router v2 — Observability & Quota-Aware Routing.md` (7 phases A–G: event model + Claude stream-json adapter, run registry/history, stderr progress renderer, instrumented worker run, budget/estimator/preflight, drain+checkpoint+handoff, TUI commands). Four owner decisions locked in the spec's "Decisions" table: **D1** TUI dependency-free (no react/ink, ANSI behind `src/tui/render.ts`) against doc §9's recommendation — the package keeps its 3 runtime deps; **D2** two new exit codes 41 `QUOTA_INSUFFICIENT` / 42 `HANDOFF_REQUIRED` with a `HandoffResult` JSON on stdout, which obliges Phase F to update the managed blocks + SKILL.md so orchestrators read them as "unfinished, bundle attached"; **D3** ship `quotaAware: true` but `refuseOnCritical: false` + `handoffOnLowQuota: false` — the §13 cost baseline has never been measured here (unlike the v0.5 quota endpoint, which was verified live first), so refusing/killing on it would block real work; `routingAdvice.wouldRefuse` in every `summary.json` is the evidence for flipping the defaults in 2.1; **D4** two releases, `2.0.0-beta.1` after Phase D then `2.0.0` after Phase G (manual 2FA publish ritual is expensive, and the beta week seeds `cost-samples.jsonl`). Phase A starts by capturing real `claude -p --output-format stream-json --verbose` fixtures — the schema is not a stable public API and is the spec's top risk. No code written yet | — |
| 2026-09-20 | **Fresh-device verification of registry 1.0.0 on the Linux dev machine** (Ubuntu 24.04, Node 22.22.1, claude 2.1.278, codex, git; clean temp npm prefix, fake HOME). PASSED without a key: six shims ship; `--version` 1.0.0; `status` finds claude+codex via `which`; `doctor` correct (`✗ Platform linux` + all other checks, exit 1) and `doctor --network` reached the real Z.ai endpoint from Linux; the three command shims detected once the prefix is on PATH; `key check`/`glm-worker`/`usage` with no key → `ERROR [10]` exit 10; `key set` → `ERROR [50]` exit 50; `project init` → created AGENTS.md + updated CLAUDE.md preserving user content, rerun and `--dry-run` both "already up to date", `project remove` restored the file and deleted only the router-created AGENTS.md, corrupt marker → `ERROR [31]` with the file untouched; dual-agent `skill install`/rerun/`remove` + missing-Codex-home warn+skip, `status` shows both rows; **MCP over real stdio**: initialize (serverInfo 1.0.0, echoed protocolVersion), ping, tools/list (4 tools), `-32601`, notification + garbage line produced no frame, stderr empty (4 frames from 6 lines); `mcp` snippet derived the server path from the temp prefix; `benchmark --dry-run`, unknown task `ERROR [2]`, `delegate` bad name `ERROR [2]`, non-repo `ERROR [30]`. `npm run build` + `npm run lint` clean; `npm test` 192/207 (15 Linux failures classified in Known gaps). **Also verified against claude 2.1.278 that v1's spawn args are still valid**: `--tools` and `--max-turns` are accepted (both present in the binary's option table) though `--max-turns` is now hidden from `--help`; and the binary contains the exact string `When using --print, --output-format=stream-json requires --verbose`, confirming the v2 spec's Phase D arg plan. Did **not** install glm-coding-router globally on this machine | — |
| 2026-09-20 | Read docs v3 (tri-agent role switching) and v4 (generic multi-provider control plane); added a "Forward compatibility with v3/v4" section to `specs/v2-architecture.md` — 8 hedges that are cheap now and impossible to backfill (H1 event envelope carries `taskId`/`provider`/`role`; H2 canonical provider id instead of `"glm"`; H3 record Claude's `sessionId` for v3 native resume; **H4 `cost-samples.jsonl` also records `provider`/`role`/`validationOk`/`retries`/`costClass` — v4 §17–18's adaptive routing is built from this file, so a field missing in v2 means v4 ships with no history**; H5 handoff.json uses v3 §17 `from`/`to` + v4 §20 `workspace`; H6 v4 §21 state names; H7 BudgetSnapshot gains `unit`/`costClass` for v3 §21 billing safety; H8 the Claude parser sits behind an `EventAdapter` interface so `codex exec --json` is one new file in v3) plus an explicit do-NOT-build list (provider SDK, role engine, TaskGraph, scheduler, policy engine, new MCP tool names, YAML config, package rename). New open decision **D5**: v3 §1 (`zai.zcode`) and v4 §5 (`anthropic.claude-code`) use incompatible provider-id schemes and v2 is about to write that id into every run record — must be settled before Phase A | — |
| 2026-09-20 | Owner decisions after the Linux verification: **D6** close the v0.2 platform debt *before* v2 — new `specs/cross-platform.md` (target 1.1.0) covering a platform policy table (win32 + linux = supported and verified, darwin = experimental and **never executed**, no Mac available), a `src/core/user-env.ts` dispatcher behind the existing `resolveZaiApiKey({readUserEnv})` seam (win32 → the current PowerShell functions **moved unchanged**, darwin → `security` keychain, linux → `secret-tool` *only when on PATH*), instructions-with-exit-0 when no store exists, platform-aware error hints via one `describeKeyStore()` helper, and the 15 Linux test failures fixed by platform-aware fixtures + `it.runIf` splits rather than deleted coverage. Design evidence gathered first: this box runs `gnome-keyring-daemon` with the secrets component and has DBUS set, but **`secret-tool` is not installed**, so the no-store path is the common Linux case, not the exception. Main risk recorded in the spec: the Windows paths cannot be executed from here, so they are *moved not rewritten* and `npm test` on Windows is a hard release gate for 1.1.0. **D5** provider ids are vendor-first per v4 §5 — `anthropic.claude-code` / `openai.codex` / `zai.zcode` (product token stays `zcode` because v3 §9 defines that provider as the Z.ai execution stack, with `glm-5.3`/`glm-5.3-flash` as models underneath); **`docs/…v3….md` §1 still says `claude.code` and needs updating to match**. Release order is now 1.1.0 → 2.0.0-beta.1 (after Phase D) → 2.0.0 | — |
| 2026-09-20 | **Live-key verification on Linux — all remaining v1 paths now closed.** The owner supplied a key for the session (used only via env, file `chmod 600` in the scratchpad, shredded afterwards; it was pasted in chat, so **rotate it**). Results with registry 1.0.0: `key check` → configured/process environment; `usage` → live quota (lite, 5h 0/2000, weekly 4318/10000); **`glm-worker`** → stdout exactly `LINUX_OK\n`, exit 0, no stray files; **`glm-review`** asked to write `PROOF.txt` → replied `NO_WRITE_TOOL`, file confirmed absent; **MCP** `glm_usage` (live quota) + `glm_worker` (`MCP_LINUX_OK`) + unknown tool `isError`, 4 valid frames, stderr empty, **no key in any frame**; **`delegate`** → real worker wrote the file inside `<repo>.glm-worktrees/linuxsmoke`, branch `glm/delegate/linuxsmoke` kept, main repo `git status` clean. Whole suite cost **9 of 2000 credits** | — |
| 2026-09-20 | **Phase A0 of the v2 spec executed early** while the key was available (on GLM credits, not Claude quota): three real `stream-json` fixtures captured against Claude Code **2.1.278** + GLM-5.3 and committed to `tests/fixtures/streams/` (`basic` 52 lines, `edit` 111, `allowed` 77), scrubbed of home paths, the installed-skill list, socket paths and session UUIDs — which also makes them deterministic. **The real stream contradicted several spec assumptions, all now corrected in `specs/v2-architecture.md`:** `system/thinking_tokens` is 83–88% of all lines (a live token counter → throttled liveness signal, never one event per line); `assistant` messages carry exactly ONE block (thinking/tool_use/text), so the original "one turn per assistant message" heuristic would have roughly doubled every turn count; `thinking` blocks carry raw reasoning and are dropped at the adapter boundary; `system/hook_started`/`hook_response` and `system/permission_denied` exist and were unaccounted for (the latter is now a first-class `ToolDenied` event); `result` is far richer than assumed (`terminal_reason`, `permission_denials`, `modelUsage`, cache-token breakdown) so `RunCompleted` reads its numbers from there; `session_id` confirmed present → hedge H3 is viable. **Trap recorded:** `result.total_cost_usd` (0.0154) and `modelUsage[].costUSD` are Claude Code applying **Anthropic's price table to a model it does not know** (`provider: "firstParty"`, `contextWindow: 200000` — both wrong for GLM, same family of wrongness as the window-enforcement override). GLM bills in plan credits. Phase E's estimator must ignore USD; only the token counts there are real | — |
| 2026-09-20 | **Defect found in published 1.0.0: the worker's Bash tool is denied on every call.** `--permission-mode acceptEdits` auto-approves edits but not shell commands, and headless `-p` has no prompt to answer, so every Bash call returns `system/permission_denied` / `"This command requires approval"` (captured in `tests/fixtures/streams/edit.ndjson`; the worker itself reported it could not run the test). v0.1 saw the symptom and filed it as a sandbox quirk — it is the permission mode. **Verified fix**, same stack + one flag: `--allowedTools "Bash(python3 *)"` → zero denials, command ran, `"test passed"` (`allowed.ndjson`). Spec written: `specs/worker-bash-permissions.md` (config-driven `worker.allowedBash` allowlist of validation commands only; `bypassPermissions` explicitly rejected as `--dangerously-skip-permissions` renamed; empty list drops Bash from `--tools` so the worker stops wasting turns). **v2 depends on this** — `ValidationCompleted` could otherwise never reach a successful state. Needs the owner's sign-off: it lets a published tool execute shell commands by default | — |
| 2026-09-20 | **1.1.0 implemented** per `specs/cross-platform.md` + `specs/worker-bash-permissions.md` (owner approved the default-on Bash allowlist). **Cross-platform:** new `src/core/user-env.ts` dispatches the per-user secret store behind the `resolveZaiApiKey({readUserEnv})` seam — win32 keeps the PowerShell code **moved, not edited**; darwin uses `security` keychain; linux uses `secret-tool` *only when on PATH*; otherwise "none", where `key set` prints the exact `export` line plus the right profile file (picked from `$SHELL`, preferring one that exists) and exits 0. `ZaiKeySource` is now `process-env | user-store`, with `describeKeyStore()` naming the concrete store in every message. `platform.ts` gained `platformSupport()`/`platformName()`; `assertWindows()` is gone from `key set`/`key remove`/`init`. Error hints are platform-aware — **F1 closed**: the no-key hint now shows the `export` line off Windows instead of pointing at a command that exits 50 there. `doctor` reports `✓ Linux 7.0.0` and **HEALTHY exit 0** on this machine, warns "experimental" on darwin, fails only on genuinely unsupported platforms. **Bash fix (F5):** `worker.allowedBash` (9 validation-only patterns, zod-rejects shell metacharacters) drives `--allowedTools Bash(<pattern>)`; empty list drops Bash from `--tools` so the worker stops burning turns on a tool it cannot use; `--no-bash` does that for one run; profiles deliberately do **not** override the allowlist (security setting, not a perf knob); the MCP `glm_worker` tool inherits it via `buildWorkerArgs` and a test asserts that. **Tests: 228 passed / 2 skipped / 25 files, green on Linux** (was 192/207). The 15 Linux failures were fixed by `tests/helpers/platform.ts` (`exeName`) plus `it.runIf(onWindows)` splits with **new POSIX counterparts** — no coverage deleted; the 2 skips are the genuinely Windows-only `.exe`-over-`.cmd` preference tests. New `tests/unit/user-env.test.ts` (17 tests, injected runners) asserts among other things that the Linux backend passes the secret on **stdin, never argv**. Build + lint clean; README/AGENTS.md platform claims corrected; version 1.1.0. **NOT done: `npm test` on Windows, which `specs/cross-platform.md` makes a hard release gate** — the Windows paths cannot be executed from this machine | — |
| 2026-09-20 | User published **1.1.0** to npm (latest; 54 files, shasum `8866cc22`, unpacked 170510 B). **Registry verification passed on Linux**: published `dist/` is **byte-identical** to a rebuild of this working tree (51 files, `diff -r` clean) and contains the new `core/user-env.js`; clean temp prefix ships all six shims; `--version` → 1.1.0; `doctor` with a key present → **HEALTHY exit 0** (was ISSUES DETECTED on 1.0.0 purely because of the platform check); `key set` prints the export guidance and exits 0 (was `ERROR [50]`); the no-key hint from the registry shim shows the `export` line (**F1 closed end to end**); `buildWorkerArgs` from the published build emits `--tools …,Bash` + **9 Bash patterns** (F5 fix shipped); MCP handshake + tools/list clean, stderr empty, serverInfo 1.1.0. Machine's global install upgraded to registry 1.1.0 — all six shims on PATH; `glm-router doctor` against the real HOME is ✓ on every check except the genuinely-absent key (exit 1) and the two optional skills (warn). **Two open risks:** (1) the published artifact has **no commit behind it** — git is still at `f38b75c` with 36 uncommitted files, so nothing anchors the tarball to a source revision (this is the same failure shape as the 0.1.0 stale-build incident, caught early only because the tree happens to match right now); (2) `npm test` on Windows was the release gate in `specs/cross-platform.md` and **1.1.0 shipped without it**, while the change touches Windows key storage — verify on the Windows machine and be ready to ship 1.1.1 | — |
| 2026-09-20 | Committed and tagged the release that was already on npm, closing the traceability gap above: `394ca1c` (roadmap docs v2/v3/v4, the three build specs, the scrubbed stream-json fixtures) and **`4a853bb`** (the 1.1.0 implementation, 29 files), tag **`v1.1.0`** → `4a853bb`. Lockfile version was out of sync (1.0.0 vs package.json 1.1.0) and was regenerated with `npm install --package-lock-only` before committing. **Verified the chain end to end**: a clean `git worktree` checkout of the tag + `npm ci` + `npm run build` produces a `dist/` **byte-identical** to the published 1.1.0 tarball — so registry `latest` is now provably the tagged source, not just the working tree that happened to match. Secret sweep before committing found no key material anywhere in the staged content (the fixtures were scrubbed at capture time). Pushed to GitHub on user request: `f38b75c..4ac7d15 main -> main` plus the new tag; `git ls-remote` confirms `refs/tags/v1.1.0^{}` → `4a853bb` and main is 0 ahead. **npm, git main and the tag are now all in sync on 1.1.0** | `394ca1c`, `4a853bb`, `4ac7d15` |
| 2026-09-20 | **First real dogfooding session: `glm-worker` used to fix its own repo, live.** Key now reaches non-interactive shells via libsecret (`sudo apt install libsecret-tools` + `key set`) — `key check` reports `Source: the system keyring (libsecret)`, closing the last 1.1.0 code path that had only unit tests with injected runners; `doctor` is HEALTHY exit 0 on Linux with the real key. **F5 proven fixed end to end**: a worker was asked to write `slugify.py` + a test and *run* it — it did, and the proof is the `__pycache__` directory it left behind (only created when Python actually imports), not the worker's own report. Then **F4** (MCP `glm_usage` printing `5x unit 3`) and **F3** (status skill rows one column right) were both delegated and landed in `1529c81`; `describeWindow` is now exported and reused so the enum mapping lives in one place, verified byte-identical against the CLI on the live endpoint. **Caught in review, not from the worker:** its F3 change broke `skill-mcp-command.test.ts`, which asserted the literal `"Claude skill     enabled"` — the old padding was *locked in by a test*. The worker had run only the file it edited and honestly reported that file green. 230 passed / 2 skipped, build + lint clean. Whole session cost **20 of 2000 credits** | `1529c81` |
| 2026-09-20 | **Harness lessons from three worker runs, two of which failed entirely — both from how they were invoked, not from glm-worker.** (1) `glm-worker "..." 2>&1 \| tail -15` loses *all* output: `tail` buffers to EOF, and when the call exceeds the Bash timeout and is moved to the background `tail` is killed before printing. Redirect to a file instead. (2) A `run_in_background` worker did not survive a Claude Code session restart — the log file was created by the `>` redirect and then nothing, zero API calls, no process. Run workers in the **foreground** with output to a file. (3) Task-packet shape matters more than length: a packet describing the *problem* ("here is the inconsistency, fix it") ran 11 minutes and produced nothing; the same fix described as *edits* ("this exact line becomes this exact line") finished in one foreground run. Describe the edit, not the problem, whenever the work is already understood well enough to describe. (4) `claude -p` in text mode emits nothing until it finishes, so even correct logging shows an empty file for the whole run — liveness could only be inferred by watching the quota tick up. That is precisely what v2 Phase D removes | — |
| 2026-09-20 | **Windows release gate for 1.1.0 finally run** (the open risk from the publish entry above), on `4f7f4d3`, Windows 11 / Node 22.22.2. `npm test` first run: **3 failed / 228 passed**, all three one defect — `describeShellExport` (`src/core/user-env.ts`) built the POSIX shell-profile path with `path.join`, so on win32 it emitted `\home\tester\.bashrc` instead of `/home/tester/.bashrc` (`user-env` ×2, `key-command` ×1). These paths are POSIX by definition (they name a shell rc file), so the fix is `path.posix.join` / `path.posix.basename`; it changes no Windows behavior, only the text Windows prints when describing a POSIX shell. After the fix: **231 passed / 1 skipped / 25 files green on Windows** (Linux was 228/2 — the two `.exe`-over-`.cmd` tests run here, one POSIX-only test skips), `npm run build` + `npm run lint` clean. **The moved PowerShell key code works unedited**: with `ZAI_API_KEY` stripped from the process env, `key check` → `Source: Windows User Environment`, and `doctor` → **HEALTHY exit 0** (every check ✓, plus the expected process-env warning). **Blocked, not a code defect:** the live `glm-worker "Reply exactly with WIN_OK"` leg cannot run — this machine's key (identical sha1 in both the process env and the Windows User Env, so not a key-source bug) is **rejected by Z.ai with 401 Authentication Failed** on both the Anthropic proxy and the monitor endpoint. That is the key pasted in chat during the Linux session and rotated afterwards; this box still holds the old one. Re-run that one command after `glm-router key set` with the current key. Shipped as **1.1.1** (`77acf27`, tag `v1.1.1`) — version + lockfile bumped, build/test/lint re-run green at 1.1.1. **Not published to npm and not pushed**: registry `latest` is still 1.1.0, so the published tarball keeps the win32 path bug in that one guidance string | `77acf27` |
| 2026-09-20 | **Windows gate closed end to end on published 1.1.1 + a new security defect (F6).** Owner published 1.1.1 (npm `latest`) and set a fresh key. Live key verified: `key check` → `Source: Windows User Environment`, `usage` → live quota (lite, 5h 29/2000, weekly 4348/10000). **`glm-worker` on Windows: stdout is byte-exactly `WIN_OK2` plus a trailing newline **(streams separated with `od -c`), exit 0 — the one telemetry line `[claude-code:unrecognized_model] {"model":"glm-5.3[1m]"}` goes to stderr, where it belongs (Claude Code appends its own `[1m]` marker to our `glm-5.3`; same family as the known USD-pricing trap). **Registry ritual passed**: clean temp prefix install of 1.1.1 ships all six shims × 3 (sh/.cmd/.ps1), `--version` 1.1.1, published `dist/` **byte-identical** to a build of the tagged source (51 files, `diff -r` clean), `doctor` HEALTHY exit 0, worker from the registry build → `REGISTRY_WIN_OK`. **F5 proven on Windows, not just reported**: a worker asked only to run `npm run proof` left a `bash-proof.txt` containing a real ISO timestamp + pid — the allowlisted Bash actually executed, no permission denial. **F6 (NEW, security, affects 1.0.0–1.1.1): `glm-review`'s read-only guarantee (spec §17) is defeated whenever the MCP server is registered.** `--tools Read,Glob,Grep` only restricts **the built-in set** (the binary's own help says so); MCP tools from user scope are additive. Asked to write `PROOF.txt`, the review session called `mcp__glm-coding-router__glm_worker` and the file was created — it then said so itself. Enumerated live, a review session sees `Glob, Grep, Read, mcp__glm-coding-router__{glm_delegate,glm_review,glm_usage,glm_worker}`: write access, recursion, and credit spend from a surface documented as read-only. Missed on Linux only because no MCP server was registered there. **Fix verified empirically before proposing**: adding `--strict-mcp-config` (with no `--mcp-config`, so zero servers) to the review spawn makes the same prompt enumerate exactly `Glob, Grep, Read` on claude 2.1.278. `glm-worker` has the same MCP inheritance (write is expected there, but recursive `glm_delegate` is not) — both spawns need it. Spec-first per repo convention; **awaiting owner sign-off, nothing implemented**. Also seen on stderr: Claude Code's notice that auto-mode classifier requests stay billed as before because the gateway is `api.z.ai` | — |
| 2026-09-20 | **1.1.2 implemented — F6 closed** per the new `specs/review-mcp-isolation.md`. One constant, `STRICT_MCP_ARGS = ["--strict-mcp-config"]` in the new `src/core/agent-args.ts`, now goes on **every headless spawn we construct**: `buildReviewArgs` (the defect), `buildWorkerArgs` (write is expected, undeclared recursion into `glm_delegate` is not) and the `benchmark` task spawn (a benchmark is only comparable with a fixed tool surface). In `buildWorkerArgs` it sits **before** `--allowedTools`, whose variadic Bash patterns must stay last — a test pins that ordering. Interactive `glm-chat`/`glm-fast` are deliberately excluded: the user's own MCP servers are theirs. Not configurable and not profile-overridable, same reasoning as `worker.allowedBash`. **Live proof on Windows, same prompt that broke it before**: `glm-review` asked to write `PROOF.txt` → replies `NO_WRITE_TOOL` and the directory is **empty**; asked to enumerate its tools → exactly `Glob, Grep, Read` (was 3 built-ins + 4 `mcp__glm-coding-router__*`). **F5 survived the fix** — a worker asked only to run `npm run proof` still left a `bash-proof.txt` with a real ISO timestamp + pid, so the allowlisted Bash still executes with strict MCP on. **Three existing tests pinned the old argv and were updated deliberately** (`prompt-args`, `glm-fast`, `benchmark-command`) — the same trap as the `skill-mcp-command` padding assertion in the dogfooding session: a test can lock in the behaviour you are trying to fix. New `tests/unit/mcp-isolation.test.ts` (7 tests) covers all three builders, the flag ordering, the Bash-disabled path and both MCP tools. **238 passed / 1 skipped / 26 files on Windows**, build + lint clean; README + AGENTS.md hard-rule bullet updated; version 1.1.2. One observation worth keeping: a worker run in a **non-git** temp dir stalled past 11 minutes producing nothing and was killed, while the identical prompt in a `git init`-ed dir finished in about a minute — matches the earlier harness lesson, and `claude -p` text mode gives no liveness signal either way (quota ticking was the only evidence it was alive)  **CORRECTED 2026-09-20 (same day): the non-git explanation above is WRONG.** The real cause was `resolvePrompt` awaiting stdin before looking at argv: under a harness whose stdin is an open pipe, `glm-worker "prompt"` blocked before doing anything at all. Proven by holding stdin open 25 s — no run directory, no API call, no output — then completing 7 s after the pipe closed. Fixed by checking args first; the git-repo correlation was coincidence. | — |
| 2026-09-20 | User published **1.1.2** to npm (`latest`; shasum `87e1c90a`, unpacked 173323 B). **Registry verification passed on Windows** — the first time this ritual has been run on this platform. Clean temp prefix ships all six shims; `--version` 1.1.2; published `dist/` **byte-identical** to a build of the tagged source (52 files, `diff -r` clean) and contains the new `core/agent-args.js`; the published `buildReviewArgs`/`buildWorkerArgs` emit `--strict-mcp-config`, correctly **before** the 9 variadic `--allowedTools` patterns. Live from the registry build: `doctor` HEALTHY exit 0; **F6 closed end to end** — the exploit prompt returns `NO_WRITE_TOOL` and creates nothing; `glm-worker` → `REGISTRY_112_OK`; MCP over real stdio → initialize (serverInfo 1.1.2) + `glm_worker` → `MCP_REG_112`, child exit 0, **stderr empty, key absent from every frame**. **Caught during the ritual: the machine's global install was still 1.1.0**, and `~/.claude.json` points the registered MCP server at that global path — so the user's own Claude Code sessions had been running the version with the F6 hole this whole time, two publishes later. Upgraded to 1.1.2; from the PATH shims `--version` 1.1.2, `doctor` HEALTHY, `glm-review` → `NO_WRITE_TOOL` with no file. **Lesson: publishing does not upgrade this machine, and the MCP registration silently pins whatever the global install is — add the global upgrade to the publish ritual, and re-check it whenever an MCP-visible defect is fixed.** Note for next time: driving `glm-mcp` from a script that keeps stdin open produced zero frames twice (the server is fine — piping the same lines works, and `stdin.end()` lets in-flight work finish before exit); use EOF-terminated stdin for MCP smoke tests | — |
| 2026-09-20 | **`project init` / `skill install` live-verified on Windows at registry 1.1.2** — the last two surfaces that had only test coverage here. Run from the **PATH shims** of the upgraded global install, against a **fake `USERPROFILE`** (`os.homedir()` honours it on Windows) and scratch repos, never the real home. **skill:** `--dry-run` names both target dirs and writes nothing; `install` → both `.claude/skills` and `.codex/skills`, and the installed `SKILL.md` is md5-identical (`2d884f8f`) to the one in the real home; rerun → `already installed` ×2; `.codex` missing → `home not detected — skipping optional skill`, **exit 0**; `remove` → both removed, zero leftovers; second `remove` → `not installed`, exit 0. **project:** `--dry-run` prints the full diff for both files and creates nothing; `init` on a repo with an existing hand-written `CLAUDE.md` updates the managed block and **preserves the user's text**, creates `AGENTS.md`; rerun and `--dry-run` → `already up to date` ×2; non-git dir falls back to cwd. **Ownership is what makes `remove` safe, and it works both ways**: the router deletes only files it created (`ownership.json` in the fake home listed exactly the one `AGENTS.md` it made) — in a repo where the **human** wrote `AGENTS.md` and the router created `CLAUDE.md`, `remove` deleted the `CLAUDE.md` and only stripped the block from `AGENTS.md`, leaving the human's text intact; afterwards `ownership.json` is `{"files": {}}`. Corrupt marker (end marker deleted) → `ERROR [MANAGED_BLOCK_CORRUPT]` **exit 31** with the file's md5 unchanged. **Real home verified unpolluted afterwards**: still 61 files under the two skills dirs, both `SKILL.md` md5 unchanged, real `ownership.json` mentions none of the three scratch repos, `config.json` mtime still 2026-09-17. F3 also visible as fixed — `status`'s skill rows line up with the policy rows. **Every v1 surface has now been executed live on Windows at 1.1.2**  **CORRECTED 2026-09-20 (same day): the non-git explanation above is WRONG.** The real cause was `resolvePrompt` awaiting stdin before looking at argv: under a harness whose stdin is an open pipe, `glm-worker "prompt"` blocked before doing anything at all. Proven by holding stdin open 25 s — no run directory, no API call, no output — then completing 7 s after the pipe closed. Fixed by checking args first; the git-repo correlation was coincidence. | — |
