# memory/2026-09-18.md — v0.3 through v1.0

Four releases in one day. `delegate` with git worktrees (v0.3), `benchmark` (v0.4), `usage`
against the live Z.ai monitor endpoint (v0.5 — the endpoint whose shape Phase E later routes
on), and v1.0: the dual-agent skill plus the hand-rolled `glm-mcp` stdio server. Ends with the
technical spec moving into `docs/`.

Archived from `MEMORY.md` on 2026-09-21. These entries are **settled history**: every version
named below is published to npm, verified from the registry and tagged. Open threads stayed in
`MEMORY.md`.

| Date | What | Commits |
|---|---|---|
| 2026-09-18 | **v0.3 implemented** per spec §54: `glm-router delegate <name>` runs a glm-worker in an isolated git worktree (branch `glm/delegate/<name>`, worktree at `<repo>.glm-worktrees/<name>` outside the repo, cut from HEAD). Spec written first: `specs/delegate-worktrees.md`. New `src/core/git.ts` + `src/core/worktree.ts` + `src/commands/delegate.ts`; 28 new tests (`tests/unit/worktree.test.ts`, `tests/integration/delegate-command.test.ts` with real git in temp repos + injected-spawn DI, incl. a real fake-agent spawn inside a created worktree). 160 tests, 19 files, all green. Version bumped to 0.3.0; publish pending user (2FA) | `bb6d44a` |
| 2026-09-18 | User published 0.3.0 to npm (latest). Verified from the registry into a clean prefix: `glm-router --version` → 0.3.0, `status`/`delegate --help` correct; real `delegate registry-smoke` in a temp git repo returned `REGISTRY_V03_OK` (exit 0, worktree + branch kept at the documented paths); same-name rerun → `ERROR [31] WORKTREE_FAILED`, bad name → `ERROR [2] INVALID_DELEGATE_NAME` from the registry shims. Upgraded this machine's global install to the registry 0.3.0. GitHub main + tag `v0.3.0` already in sync | — |
| 2026-09-18 | **v0.4 implemented** per spec §54: `glm-router benchmark` measures the Claude+GLM stack on a built-in task suite (`fn-reverse`, `fix-bug`) — each task runs in a throwaway temp dir through the standard glm-worker path + `--output-format json`; metrics: duration, GLM calls (num_turns), tokens, tests (validate cmd), success, intervention; retries render "-" (not exposed by Claude Code; counting proxy is future work). Report JSON saved under `<configDir>/benchmarks/`. Quota guard: interactive confirm, `--yes` required non-interactively. Codex stack recognized but `ERROR [2]` (not drivable headless yet). Spec first: `specs/benchmark.md`. 19 new tests; `spawnAgentCapture` added to process.ts (shared signal-forwarding core); fixture learned `GLM_TEST_RESULT`. 179 tests, 21 files, all green. Version 0.4.0; live-verified `benchmark --yes --task fn-reverse` → 39.0s wall clock, 6 turns, 2028/984 tokens, tests PASS, report saved correctly (claude duration_ms 37706 also captured); publish pending user (2FA) | `a48464b` |
| 2026-09-18 | User published 0.4.0 to npm (latest). Verified from the registry into a clean prefix: `--version` → 0.4.0, `benchmark --help`/`--dry-run` correct, `--stack codex` and unknown task → `ERROR [2]`; real `benchmark --yes --task fn-reverse` → 50.2s / 7 turns / 4927–1206 tokens / tests PASS, report saved under the config dir. Upgraded this machine's global install to the registry 0.4.0. GitHub main + tag `v0.4.0` already in sync | — |
| 2026-09-18 | **v0.5 implemented** per spec §54: `glm-router usage` — Z.ai Coding Plan quota via the monitor endpoint `GET https://api.z.ai/api/monitor/usage/quota/limit` (Bearer key; the API Z.ai's own dashboard uses; **verified live** before designing: 5h window = `unit:3,number:5`, weekly = `unit:6,number:1`, confirmed by reset-timestamp spacing; `model-usage` endpoint returned empty body → not used); local totals aggregated from saved benchmark reports; Claude/Codex render explicit "not available" lines (no headless usage surface exists — checked both CLIs). Spec first: `specs/usage.md`. 9 new tests (`tests/integration/usage-command.test.ts`, real payload shape as fixture). 188 tests, 22 files, all green. Live-verified with real key (lite plan, both windows). Version 0.5.0; publish pending user (2FA) | `74840e9` |
| 2026-09-18 | User published 0.5.0 to npm (latest). Verified from the registry into a clean prefix: `--version` → 0.5.0; real `usage` run from the registry shim rendered the live quota (lite, 5h 1137/2000 56%, weekly 3528/10000 35%) and local benchmark totals, exit 0; `usage --json` valid. Upgraded this machine's global install to the registry 0.5.0. GitHub main + tag `v0.5.0` already in sync | — |
| 2026-09-18 | **v1.0 implemented** per spec §54: (1) dual-agent skill — `glm-delegation` SKILL.md now installs into `~/.claude/skills` AND `~/.codex/skills` (`ClaudeSkillInstaller` sharing a base class; per-agent skip when home missing; status shows both rows, doctor checks both); (2) `glm-mcp` (6th binary) — hand-rolled stdio JSON-RPC MCP server (initialize/ping/tools subset, **no new npm dep**) exposing `glm_worker`/`glm_review`/`glm_delegate`/`glm_usage` as MCP tools built on core primitives only (never CLI commands — stdout is the protocol channel); (3) `glm-router mcp` opt-in registration via `claude mcp add/remove -s user` (never edit ~/.claude.json ourselves; server path derived from module location). Spec first: `specs/v1-architecture.md`. 19 new tests. Live-verified: protocol handshake + tools/list over real stdio, real `glm_usage` (quota live) and `glm_worker` ("MCP_OK") tool calls through glm-mcp, real `skill install` wrote ~/.claude/skills/glm-delegation (Codex was already there), `mcp` snippet correct. 207 tests, 24 files, all green. Version 1.0.0; publish pending user (2FA) | `1f85569` |
| 2026-09-18 | User published 1.0.0 to npm (03:41Z, latest; 53 files, tarball `38addc57`). **Registry verification passed**: clean temp prefix install ships all six shims (`glm-router/chat/fast/worker/review/mcp`); published `dist/` byte-identical to a fresh `npm run build` of `c18250b` (50 files, so the tarball is the current source, unlike 0.1.0); `--version` → 1.0.0; `status` shows both skill rows; `doctor` HEALTHY incl. the new Claude delegation-skill check; `mcp` snippet derives the server path from the module location (temp prefix and, after upgrading, the nvm global dir); `glm-mcp` over real stdio → initialize (echoed protocolVersion + serverInfo 1.0.0), ping, tools/list (4 tools), unknown method `-32601`, notification/garbage line produce no frame, stderr empty; real tool calls `glm_usage` (live quota, lite 5h 1922/2000, weekly 4313/10000) and `glm_worker` → `MCP_REG_OK`, unknown tool → `isError`; dual-agent `skill install` → both `.claude`/`.codex` homes in a **fake** USERPROFILE, second run no-op, `skill remove` cleans both, missing Codex home → warn+skip exit 0, real home confirmed untouched. Upgraded this machine's global install to registry 1.0.0 and re-ran from the PATH shims: `glm-worker` → `REGISTRY_V1_OK`, `glm-review` lists only Glob/Grep/Read. Local suite re-run on HEAD: 207 tests / 24 files green, build + lint clean; GitHub main + tag `v1.0.0` in sync. **Not done:** real `glm_delegate` worker run through MCP (5h window was at 96% — the CLI `delegate` path was already verified from the registry at 0.3.0) and `glm-router mcp install` (opt-in; writes the user's Claude config — not registered). **Nit found:** `status.ts:74` pads the skill rows with a hardcoded 5 spaces, so `Claude skill` sits one column right of `Claude policy` | — |
| 2026-09-18 | User ran `glm-router mcp install` themselves (the command writes `~/.claude.json`, so the agent's own sandbox blocks it as self-modification — hand it to the user). Registered fine: `claude mcp get glm-coding-router` → User-config scope, stdio, `✔ Connected`, command = `node.exe <nvm global>\glm-coding-router\distin\glm-mcp.js`. Tools only show up in Claude Code sessions started **after** registration. Undo: `claude mcp remove glm-coding-router -s user` | — |
| 2026-09-18 | Moved the technical spec into `docs/` (`git mv`, rename tracked, content untouched) and updated the three prose references to it (`AGENTS.md`, `MEMORY.md`, `README.md`). `specs/` stays where it is: `docs/` holds the frozen v0.1 spec, `specs/` holds the per-feature specs written after it. No code or test reads the spec path; 207 tests + lint still green | — |
