# MEMORY.md

Shared working state for whoever picks up this repo next (Claude Code, Codex, or a human) —
read it before starting a task, update it briefly after finishing one. Full rules live in
`AGENTS.md` / `CLAUDE.md`; technical detail lives in
`docs/GLM Coding Router — Technical Specification v0.1.md`. This file only holds what isn't already
there: current status, decisions made *outside* the spec, and constraints discovered while
building. Keep it under ~150 lines — if it grows past that, split older entries into
`memory/YYYY-MM.md` and leave a one-line pointer here.

## Current status

Checked against the spec's Definition of Done (§51) on 2026-09-17 — code read + `npm run build` /
`npm test` / `npm run lint` actually run, not just "file exists":

| Item (§51) | Status |
|---|---|
| Package installs globally | ✓ `0.1.1`–`1.0.0` published to npm (user runs the 2FA-gated publish manually; latest = **1.0.0**, tags v0.1.1–v1.0.0 pushed to GitHub). All verified from the registry: installed into clean temp prefixes → binaries work, `glm-worker` returned `REGISTRY_OK`/`V02_OK`; 0.3.0's `delegate` ran a real GLM worker returning `REGISTRY_V03_OK` with worktree+branch kept, and the ERROR [31]/ERROR [2] paths fired correctly from the registry shims; 0.4.0's `benchmark` ran a real GLM run returning PASS (50.2s / 7 turns) with the report saved, dry-run + INVALID_ARGS guards correct; 0.5.0's `usage` rendered the real quota from the monitor endpoint (text + --json); 1.0.0 shipped all **six** shims incl. `glm-mcp`, published `dist/` is byte-identical to a rebuild of `c18250b`, and the registry build passed MCP handshake + real `glm_usage`/`glm_worker` tool calls, dual-agent `skill install`/`remove` into a fake home, and `glm-worker`/`glm-review` from the PATH shims; machine's own global install tracks the registry (now 1.0.0). Fresh-machine install (§50) remains blocked on hardware |
| Four CLI binaries work | ✓ implemented, exercised via `tests/fixtures/fake-agent.mjs` — six since v1.0 (`glm-fast`, `glm-mcp`) |
| ZAI key setup works | ✓ `key set`/`key check`/`key remove` tested end to end (`tests/integration/key-command.test.ts`) via injected `prompt`/`setEnv`/`deleteEnv` — real key set once manually via `glm-router key set` earlier in the project's life, real machine confirms `doctor`/`status` see it |
| Stale Orca environment handled | ✓ `resolveZaiApiKey()` unit-tested (process env → Windows User Env → fail) |
| Claude detection works | ✓ `locateClaude()` — discovery order (where.exe → PATH → override → error) unit-tested with an isolated PATH (`tests/unit/claude-discovery.test.ts`) |
| Codex detection works | ✓ `locateCodex()`/`codexRequired()` — same test file, same isolation technique |
| Interactive GLM works (`glm-chat`) | ✓ unit-tested; `glm-chat --version` run live from the fresh-prefix install — printed `2.1.274 (Claude Code)`, confirming the interactive spawn path (2026-09-17) |
| Worker GLM works | ✓ integration-tested, **and run for real**: `glm-worker "Reply exactly with WORKER_OK"` on this machine returned exactly `WORKER_OK`, exit 0 (2026-09-17) |
| Read-only GLM works | ✓ integration-tested, **and run for real**: asked `glm-review` to create a file — it reported it has no Write/Edit/Bash tool available and the file was confirmed absent afterward (2026-09-17) |
| stdin works | ✓ tested (stdin → args → error priority) |
| `project init` idempotent | ✓ tested (second run is a no-op) |
| `project remove` safe | ✓ tested (preserves user content, deletes only router-owned empties) |
| `doctor` works | ✓ Agents/Z.ai key logic isolation-tested; **and run for real** — `glm-router doctor` and `glm-router doctor --network` both report HEALTHY with the real Z.ai endpoint reachable (2026-09-17). Text/JSON rendering and the `--network` probe (spec §42) now covered end to end via injected `fetchImpl` (`tests/integration/doctor-command.test.ts`) |
| `status` works | ✓ tested end to end (JSON + text), **and run for real** — `glm-router status` matches the real machine's state |
| `uninstall` works | ✓ tested end to end (`tests/integration/uninstall-command.test.ts`) — config removal, key-kept-by-default, and managed-block stripping via `deps.root`; the interactive "remove key" wizard path is untestable without mutating real stdin, documented as skipped in the test file |
| Secrets never logged | ✓ tested (`redact()`); `status` also asserted to never print the key value |
| Unit tests pass | ✓ 114/114, 15 files (confirmed 2026-09-17) |
| Integration tests pass | ✓ same run, fake-agent based |
| README complete | ✓ covers every §52 section |

**Known gaps — all remaining items are BLOCKED on hardware** (no code work pending):

- **[BLOCKED — needs a fresh Windows machine]** §50 acceptance criteria: everything verified on
  *this* machine (2026-09-17) — `doctor`/`doctor --network`/`status` HEALTHY with the real
  endpoint reachable, `glm-worker` returned `WORKER_OK` (and later `REGISTRY_OK`/`V02_OK` from
  registry installs), `glm-review` confirmed it has no write-capable tool and created nothing,
  `project init --dry-run` reports "already up to date" on installed blocks without touching
  files. Registry installs of 0.1.1 and 0.2.0 verified into clean prefixes, incl. the
  simulated-fresh-machine pass (no claude/codex/git states, fake home). Still open: run the same
  suite on an actual **fresh** Windows machine via `npm install -g glm-coding-router`.
- **[BLOCKED — needs Win10 hardware + Orca terminal]** §49 Windows test matrix: PowerShell axis
  verified as far as this machine allows — the code always spawns `powershell.exe` (5.1) for key
  resolution regardless of parent shell, and `glm-router key check` was run from a PowerShell 7.6
  parent successfully. Still open: real Win10 machine, Orca embedded terminal.

- **[NEW 2026-09-20 — Linux dev machine]** A fresh-device install of registry 1.0.0 was run on
  an Ubuntu 24.04 box (Node 22.22.1, claude 2.1.278 + codex + git present, no prior
  `~/.glm-coding-router`). Everything that does not need the Z.ai key passed — see the session
  log. Three open items came out of it:
  1. **Dead-end hint on non-Windows.** No key → `ERROR [10]` says "Run: glm-router key set",
     but `key set` is `assertWindows()`-gated → `ERROR [50]`. The hint should be
     platform-aware (`export ZAI_API_KEY=…` on Linux/macOS).
  2. **15 of 207 tests fail on Linux** (build + lint clean): 6 are test-only fixtures writing
     `claude.exe`/`codex.exe`/`.cmd` names that `searchPathFor` correctly ignores off Windows
     (`claude-discovery` ×5, `status` ×1); 5 hit the real `assertWindows()` gate in
     `key set`/`key remove` (`key-command` ×3, `init-command` ×2); 4 assert `doctor` is
     HEALTHY, which cannot hold while the platform check reports `✗ Platform linux`
     (`doctor-command` ×3, `doctor` ×1). **`npm test` is therefore not a usable green gate
     for v2 work on this machine until this is resolved.**
  3. `status.ts:74` skill-row padding nit (known) reproduced here.
- **[CLOSED 2026-09-20]** The live-key paths all passed on Linux — see the session log. Two
  more defects surfaced while doing it: **the worker's Bash tool is denied on every call**
  (`specs/worker-bash-permissions.md`, affects published 1.0.0) and the MCP `glm_usage` tool
  renders raw quota enums (`5x unit 3`) where the CLI renders `5-hour window`, because
  `src/mcp/server.ts:185` builds its own label instead of using `describeWindow`, which
  `src/commands/usage.ts:45` never exported.

When the hardware is available: for §50, follow the checks listed above on the fresh machine
(MEMORY.md carries the full context); for §49, repeat the key checks under PS 5.1/7.x on Win10
and run `glm-worker` inside an Orca embedded terminal.

## Decisions made outside the spec

| Date | Decision | Why |
|---|---|---|
| 2026-09-17 | Set `CLAUDE_CODE_DISABLE_UNKNOWN_MODEL_WINDOW_ENFORCEMENT=1` in the child env (`src/core/env.ts`) | GLM models aren't in Claude Code's model catalog, so it was enforcing an incorrect 200k context window on them |
| 2026-09-17 | `locateClaude`/`locateCodex`/`codexRequired` now take an optional trailing `env` param (default `process.env`), threaded into `where.exe`/`which` and `searchPathFor` | Lets tests fully replace `PATH` to isolate discovery from whatever is actually installed on the machine running the tests, without mocking modules — matches the project's existing "real fs/temp-dir, no `vi.mock`" test style |
| 2026-09-17 | `runDoctorChecks()` now also takes optional `env` and `readUserEnv`, threaded into `locateClaude`/`locateCodex`/`resolveZaiApiKey` and the process-key check | Same reasoning as above, applied to `doctor` — it was the only remaining caller of these primitives still hardcoded to the real environment |
| 2026-09-17 | `statusCommand()` takes a second `deps: {home, env, readUserEnv}` param (default `{}`, unchanged production behavior) | Same technique again; `status` was the last read-only command still hardcoded to `os.homedir()`/`process.env` |
| 2026-09-17 | `key.ts`/`init.ts`/`uninstall.ts` also take injectable `deps` (`prompt`, `setEnv`, `deleteEnv`, `home`, `root`, `env`) and no longer call `process.exit()` internally — `askChoices()` in both `init.ts` and `uninstall.ts` returns a `{kind: "ok"\|"non-interactive"\|"cancelled"}` union instead | `process.exit()` inside a function makes it untestable (kills the test runner); the exit-code decision now belongs to the caller (`initCommand`/`uninstallCommand`), which is how every other command already worked |
| 2026-09-17 | This refactor + all 4 new command test files were delegated to `glm-worker` per this repo's own delegation policy, in 6 dispatches (2 full-scope attempts that hit `--max-turns` and were abandoned, then split one-file-at-a-time after bumping `worker.maxTurns` to 40) | The first two attempts tried to do all 4 files' tests in one call and ran out of turns before writing anything; splitting by file fit the budget. Every dispatch's diff was read and `npm run build`/`test`/`lint` re-run manually before accepting — the worker itself could not run these (Bash was declined in its headless sandbox) |
| 2026-09-17 | `doctorCommand()` now takes a second `deps: {home, env, readUserEnv, fetchImpl}` param (default `{}`, unchanged production behavior); `probeEndpoint()` takes an injectable `fetchImpl` param | Closed the last doctor test gap noted above — same DI technique as `statusCommand`. Found and fixed a real bug while doing it: `doctor --network --json` never ran the network probe at all (JSON branch returned before computing `networkResult`); JSON output now includes a `network` field when `--network` is passed |
| 2026-09-17 | v0.2: our `--profile` flag is consumed by the four task binaries and intentionally shadows Claude Code's own `--profile` inside them; unknown profiles reuse `Errors.configInvalid` (exit 11) rather than a new code | Keeps one config-error family; claude users needing claude's profile flag must call `claude` directly. Design details in `specs/glm-fast-profiles.md` |
| 2026-09-18 | v0.3 delegate: worktrees live **outside** the repo at `<repo>.glm-worktrees/<name>`; worktree + branch are **kept** after every run; `--remove` fires only after success via plain `git worktree remove` (git's dirty-tree refusal is the data-loss guard); when `--profile` is absent, a profile literally named `<name>` applies implicitly; delegate error names reuse §35 families — `GIT_NOT_FOUND`/`GIT_REPO_REQUIRED` → 30, `WORKTREE_FAILED` → 31, `INVALID_DELEGATE_NAME` → 2 | Sibling-dir placement means no `.gitignore` edits ever; keep-by-default honors the no-automatic-git-commits rule (the only branch deletion is rolling back the branch this same invocation created, when the worker never spawned). Design in `specs/delegate-worktrees.md` |

## Constraints discovered

- Orca terminals snapshot the environment at startup — a key added after Orca starts is
  invisible to those terminals, hence the Windows-User-Environment fallback in
  `resolveZaiApiKey()` (`src/core/zai-key.ts`) is read on every invocation, never cached.
- Standalone Claude Code installs expose `claude.exe`, not a `.cmd` shim — don't assume the
  shim exists when locating the binary (`src/core/claude.ts`).

## Session log

Entries for **v0.1 → 1.1.2** (publishes, registry verifications, the Linux and Windows
platform gates, the F1–F6 defect trail) are archived in [memory/2026-09.md](memory/2026-09.md).
Everything below is v2 work in progress.

| Date | What | Commits |
|---|---|---|
| 2026-09-20 | **v2 Phase A complete on branch `v2/phase-a`** — event model + bus (`bddfc05`), Claude stream-json adapter (`db28256`). Suite **272 passed / 1 skipped / 28 files**, build + lint clean. Nothing is wired yet: no command emits these events; Phase B (run registry/store) and D (instrumented run) do that. **This was also the first session where glm-worker did the actual implementation work on this repo** — three delegated packets, each reviewed here against the real diff and a full `npm test` (not the worker's own narrower run). **Two real defects in my own spec were found by the worker, not by me:** (1) Phase A's body table still described the pre-hedge envelope `{runId, seq, ts}` with `provider: "glm"`, contradicting H1/H2 and decision D5 in the same document — it flagged this as an orchestrator decision rather than silently choosing, which is the right boundary; the envelope is now `{runId, taskId, provider, role, seq, ts}` with the constant `zai.zcode` (not a parameter, so a wrong id cannot enter the history), and Phase E's BudgetSnapshot was corrected too. (2) **The validation regex matched none of A0's captured commands** (`python3 test_add.py`), so no fixture exercised the validation path at all and the spec's "a denied validation is a real, reportable outcome" described an unreachable state; the regex gained a `python3` arm agreeing with `specs/worker-bash-permissions.md`, and two fixture assertions flipped — `allowed.ndjson` now validates **green**, `edit.ndjson` validates **red** with 2 denials, which is finally the contrast both fixtures were captured for. **Things the real stream settled:** turn derivation is the `tool_result` → next `tool_use` cycle (derived 1/4/2 vs `num_turns` 2/5/2 — the result stays authoritative, and the spec's "reconcile at the end" is **not** implemented, deferred to the summary phase); `permission_denied` arrives **before** the error `tool_result`, so dropping the pending entry is load-bearing, not polish. Architecture call made before delegating: the adapter emits **unstamped `EventInput[]`**, never `WorkerEvent[]` — the bus is the only authority for the envelope. **Delegation lessons (cost ~400 credits for the three packets):** the Bash allowlist shapes the packet — `npx tsc`, `npx eslint`, `grep`, `cat` are not on it, so validation must be phrased as `npm run build` / `npm run lint`, and saying so up front stops the worker burning turns on blocked commands; a worker validates **only what you tell it to**, so `npm test` must be an explicit acceptance criterion or it runs just its own new file (exactly the trap from the 1529c81 session); packets that say “this exact shape” finish fast, while the biggest packet (the adapter) ran >10 minutes with **no output at all** until it finished — quota ticking was again the only liveness signal, which is precisely what Phase D exists to fix | `bddfc05`, `db28256` |
| 2026-09-20 | **v2 Phase B complete** on `v2/phase-a` — foundations (`ef943bf`: monotonic ULID, run path helpers, defaulted `history` config) and the registry/store/heartbeat (`38dfa6a`). Suite **307 passed / 1 skipped / 32 files**, build + lint clean. Still unwired: no command creates a run; Phase D does that. **C3 hole found in review, not by the worker:** `taskTitleOf` redacted only against `process.env.ZAI_API_KEY`, which on Windows is empty **by design** — the key lives in the User Environment precisely so it stays out of the process. That redaction would have covered nothing in the deployment we ship to. It now takes the caller's resolved secrets with the env var as fallback, plus a test in the production shape (secret absent from env, still redacted). The worker's own C3 test is good and stays: it plants a secret in both env and prompt, then **walks the whole `runs/` tree twice**, before and after `finishRun`. **Two product defects surfaced by using the tool for real work:** (1) **a worker that hits `maxTurns` reports nothing it accomplished** — packet B1 died with `Error: Reached max turns (20)`, exit 1, yet all five files were already written and correct (build/test/lint green when I ran them myself). Trusting the exit code would have thrown away a complete, correct change; the ceiling should produce a summary of work done. (2) **`config set` cannot create a profile** although `ProfileSchema` supports `workerMaxTurns`/`reviewMaxTurns` — `config set profiles.deep.workerMaxTurns 60` → `ERROR [11] key does not exist` (only two-level keys are handled), so the file must be hand-edited; `config show` also never prints the `profiles` section, so there is no CLI way to see which profiles exist. Added a `deep` profile (60 turns) by hand to the real config; `glm-worker --profile deep` then worked. **Packet-writing lessons:** my packet pointed at `src/core/atomic-write.ts` — the helper is actually `src/project/atomic-write.ts`; the worker found the real one and said so rather than creating a duplicate. Splitting Phase B into two packets worked: the 5-file B1 blew the default turn ceiling, the larger B2 finished comfortably at 60 turns | `ef943bf`, `38dfa6a` |
| 2026-09-20 | **v2 Phase C complete** (`97cfb0e`) — stderr progress renderer in three modes (rich box+turn-tree redrawn in place on a TTY, nested `[GLM] …` lines for orchestrators, off), all ANSI confined to `src/tui/render.ts`, plus the defaulted `ui` config section. Suite **330 passed / 1 skipped / 33 files**. Nested line formats are pinned literally because a parent agent may parse them. **Review lesson worth keeping: the worker's C1/C2 test could not fail.** It created a MemoryStream for stdout, never passed it to anything, and asserted it was empty — vacuously true forever. Rewrote it to spy on the **real** `process.stdout.write` during the replay and to also assert the renderer wrote something to stderr (so "nothing rendered" cannot pass either). **Then mutation-tested it**, which produced its own lesson: the first mutation (leak inside `Writer.write`) left the test green — not because the guarantee held but because **nothing calls `Writer.write`; the renderers go through `Writer.line`**. Mutating `line` failed the test as it should. A green mutation test can mean the mutation missed the live path. Worker again reported its departures from doc §7 rather than hiding them (file count + decimal seconds on the completion line, summary instead of a bare word on the turn line, `● Connected`/`● Agent initialized` omitted) — all deliberate, packet over doc, since the doc predates the spec. **Phase A–C are done and none of it is wired**: no command creates a run yet. Phase D is the wiring, and `src/core/process.ts` is the obstacle to know about — `spawnAgent` uses `stdio: inherit`, so the parent never sees a byte; Phase D needs the new `spawnAgentStream` that pipes and line-splits, with `spawnAgentCapture` (buffer-until-exit) unsuitable because progress must be live | `97cfb0e` |
| 2026-09-20 | **v2 Phase D complete — the router now watches itself** (`87fa58f` streaming spawn, `788a695` orchestration, `6bb6c96` wiring). `glm-worker`/`glm-review`/MCP branch once on `shouldObserve`; the v1 path stays byte-identical when a caller passed `--output-format` or set `GLM_ROUTER_OBSERVE=off`. Suite **355 passed / 1 skipped / 36 files**. **Verified live, not just in tests:** a real task prints `[GLM] ● #21T9 started • glm-5.3 … ✓ #21T9 • 5.6s • 1 turns • 0 files` on **stderr**, exactly the final text on **stdout**, and leaves `runs/history/<date>/<runId>/{events.jsonl,summary.json}` behind. **The big one: the mystery stalls all session were OUR bug, and the earlier diagnosis in this file was wrong.** `resolvePrompt` awaited stdin *before* looking at argv, so under any harness whose stdin is an open pipe (agent, CI, `nohup`) `glm-worker "prompt"` blocked **before doing anything** — no run dir, no API call, no output. Measured: stdin held open 25 s → nothing; pipe closes → done in 7 s. Fixed by checking args first and never awaiting stdin when they carry a prompt (piping a packet with no args is unchanged); README/AGENTS.md precedence corrected to args → stdin → error, and the test that pinned the old order was inverted deliberately with the reason in a comment. The three MEMORY rows that blamed “non-git directory” are now marked CORRECTED — that correlation was coincidence. **Second UI defect found by looking at real output, not by tests:** the rich header box was two characters short (rows are `│ … │`, borders were drawn at content width) and all 23 tests passed anyway, because they asserted `toContain("╭─ GLM Worker")` — the opening glyphs, not the shape. Fixed, plus a geometry assertion verified to fail on the old code. That is **twice in one phase** a green test hid a real defect, both the same shape: asserting what is easy to reach instead of the guarantee. Worker notes worth keeping: it gated MCP observation on `deps.spawn === undefined` because six existing tests inject a spawn and pin v1 argv (production never injects, so production observes); and it noticed my concurrent edits to `src/tui/progress.ts` mid-run, left them untouched, and asked me to reconcile — correct behaviour for a file outside its allowed list | `87fa58f`, `788a695`, `6bb6c96` |
| 2026-09-20 | **v2 Phase G complete** (`8c12e6f` runs family, G2 watch+dashboard, `bbbaee0` summarize fix) and the three defects found by dogfooding were triaged — **one of them turned out to be my own wrong claim**. Suite **402 passed / 1 skipped / 39 files**. **(1) “Bash allowlist matches by prefix, so `npm test | anything` escapes it” — FALSE, retracted.** Probed it properly with two live runs: `npm test && node -e "…writeFileSync…"` and the same payload behind a pipe both came back *“This Bash command contains multiple operations. The following part requires approval: node -e …”*, nothing executed, no file created. Claude Code splits compound commands and checks every segment; what I had seen running (`npm test 2>&1 | grep … | tail`) was allowed because **it** treats `grep`/`tail` as safe, not because our pattern leaked. `specs/worker-bash-permissions.md` needs no change. Worth remembering: the worker also **refused to route around the denial** via its Write tool and said why — correct behaviour under a denied permission. **(2) Validation detector fired on filenames — real, fixed.** `ls eslint.config.* vitest.config.*` was rendered as “running tests” + “✓ tests passed” because the regex matched anywhere in the string. It now splits the command on `&&`/`||`/`;`/`|` and requires a runner at the **start of a segment**; 3 new tests cover filename false positives, plain runs, and a runner in a later segment. **(3) `runs show <suffix>` called every live run ambiguous — real, fixed.** A running run is in `listActive` *and* `listHistory` (its directory exists from the first event), and the code summed list entries instead of distinct ids. Candidates are now unique ids, preferring the active entry. **Same family, also fixed:** the `runs` listing showed a live run as `CRASHED`, because a stream with no terminal event derives that way — the active file's state now overrides the derived one. Note: `runs show` already preferred the live state; my first test failed only because the seed omitted the required `heartbeatAt` — my bug, not the product's | `8c12e6f`, `bbbaee0` |
| 2026-09-21 | **v2 Phase E complete — the router now decides before it spawns** (`20c22f3`). `src/core/zai-quota.ts` (monitor client moved out of `commands/usage.ts`, byte-identical, killing a duplicate interface TS had been silently merging), `budget/manager.ts` + `budget/estimator.ts`, the pure `routing/glm-routing.ts`, preflight wired into `worker-run.ts`, and `core/routing-flags.ts` for `--model|--force|--refresh-quota`. Exit codes 41/42 added. **537 passed / 1 skipped / 44 files.** **Verified live, and the live run is the story:** weekly window at 20% → zone CONSERVE → the run was downgraded to `glm-5.3-flash` (confirmed independently by Claude Code's own `unrecognized_model` telemetry, not just our own log line), stdout byte-exactly `PHASE_E_OK`, and `routingAdvice` recorded `estimatedCost 31.2` against `actualCredits 1`. **The doc §13 baseline overestimated a trivial task ~31x** — the first real check of that table on this stack, and the first hard evidence that D3's choice to ship `refuseOnCritical: false` was right: enabled, it would have refused a 1-credit task and sent the user hunting for `--force`. Do not flip it without weeks of `routingAdvice`, and re-derive the baseline from `cost-samples.jsonl` when flipping. **Two review catches the workers' own tests missed:** (1) a 200 response carrying no recognizable window mapped to an *exact* all-zero snapshot, which `zoneFor` reads as CRITICAL — fail-open defeated through a side door, on a state `usage.ts` already renders as "(no quota windows reported)"; (2) the existing test for that path passed either way, because an `unknown` snapshot also has zero windows — it could not tell the defect from the fix. Both fixed, then **mutation-tested** (neuter the guard → exactly the 3 new tests fail). Same treatment for the wiring: 3 mutations, 1/4/2 failures. **My own coordination error, worth not repeating:** I edited the tree (nợ 3) while a worker was running a packet whose acceptance criterion was "test count must stay 530/43" — I moved it to 537/44 underneath it. The worker diagnosed it exactly (+7, +1 file, named the file, attributed it to a concurrent process) and touched nothing of mine, but the packet was unfalsifiable for minutes. Either freeze the tree during a packet or state counts as "≥", never "=". | `20c22f3` |
