Create `D:\Projects\GLM-coding-router\AGENTS.md` (no `.zcode/AGENTS.md` or `.agents/AGENTS.md` exists, and no root AGENTS.md exists yet).

Since the repo is spec-only (single file: `GLM Coding Router — Technical Specification v0.1.md`), the AGENTS.md will distill the spec's binding rules for future agents:

1. **Project purpose** — npm CLI (`glm-coding-router`, binaries `glm-router`/`glm-chat`/`glm-worker`/`glm-review`) that lets Claude Code/Codex orchestrate GLM Coding Plan workers via Z.ai's Anthropic-compatible endpoint. Windows 10/11 only in v0.1, Node >= 20, TypeScript, ESM.
2. **Read the spec first** — point agents to the spec file as the authoritative source before implementing.
3. **Planned structure** — src/cli, src/bin, src/commands, src/core, src/integrations, src/project, src/templates, tests (from spec §30).
4. **Build/test commands** — npm scripts from spec §31 (`dev` tsx, `build` tsc, `test` vitest run, `lint` eslint). Note: package.json not yet created.
5. **Hard rules** — security invariants (never log/persist ZAI key, redact secrets, env injection only into child claude.exe, spawn not exec, no shell:true), key resolution order (process.env → Windows User Env → fail, for Orca stale-env), managed-block idempotency (never duplicate, preserve outside content, atomic write), standard exit codes, model names from config only, implementation order (core runtime before wizard/skill).
6. **Gotchas** — CRLF/LF handling in managed blocks, claude.exe vs claude.cmd discovery, stdin-over-args prompt priority, no `--dangerously-skip-permissions`.